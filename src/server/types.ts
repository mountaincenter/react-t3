import {
  type User as PrismaUser,
  type Weight as PrismaWeight,
  type MealPhoto as PrismaMealPhoto,
} from "@prisma/client";

export type User = PrismaUser;

export type Weight = PrismaWeight;

export type MealPhoto = PrismaMealPhoto;
